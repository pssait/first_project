package interview24.mphasis.question1;

public class Solution {
    //2 collection Employee db , department from db
    // emp id name dep id , dep id dep name
    //
    public static void main(String[] args) {
        // List<Employee> emp = from db
        // List<Department> dep = from db
        // emp.stream().map(e -> {
        // return e.getEmpName + " : "+ getDep(e.getDepId).getDepName};).forEach(System.out::println);


        // Department getDep(Integer id) {
        // Department result = null;
        // dep.stream().forEach(dep -> {
        //  if(dep.getId.equals(id) { result = dep;}
        // })}
    }
}
